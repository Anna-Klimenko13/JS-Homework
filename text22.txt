/* ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ ПОИСКА ПО МАКЕТУ */

/* Внешний контейнер (серая плашка) */
.search-block {
    width: 100% !important;        /* На всю ширину */
    max-width: 100% !important;
    height: auto !important;       /* Высота зависит от контента */
    padding: 32px 0 !important;    /* Отступы сверху/снизу всей панели */
    left: 0 !important;
    transform: translate(0, 0) scale(1) !important; /* Убираем центрирование через transform */
    top: 100px !important;         /* Размещаем ровно под шапкой */
    background: #F8F8F8 !important; /* Серый фон как в макете */
}

/* Обертка для центрирования содержимого */
.search-block_wrapper {
    max-width: 1390px;             /* По ширине твоего контейнера */
    margin: 0 auto;
    padding: 0 15px;
    display: flex;
    justify-content: center;
}

/* Сама белая строка ввода (тот самый Frame 28634) */
.search-block_input {
    width: 627px !important;       /* Точная ширина из Figma */
    height: 42px !important;       /* Точная высота из Figma */
    background: #FFFFFF !important; /* Белый фон */
    padding: 9px 16px 9px 56px !important; /* Отступы из Figma + место под лупу */
    border: 1px solid #E0E0E0 !important;
    font-size: 14px !important;
    line-height: 24px !important;
    display: block;
}

/* Иконка лупы внутри строки */
.search-block_wrapper::before {
    left: calc(50% - 295px) !important; /* Позиционируем лупу точно внутри белого поля */
    top: 9px !important;
    z-index: 5;
}
